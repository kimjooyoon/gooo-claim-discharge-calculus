# v0.1.1 preservation-forward release

`v0.1.1` is the preservation-forward release after the operational refutation
recorded in [`operational-refutations.md`](operational-refutations.md). It is
created from a PR-merged `main` commit, created as a draft first, published
once, and verified against the repository immutable-releases policy. The
workflow does not delete or replace the existing `v0.1.0` tag, release, or
assets.
